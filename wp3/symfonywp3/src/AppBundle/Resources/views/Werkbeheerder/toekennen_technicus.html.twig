{% extends "::base.html.twig" %}

{% block title %}AppBundle:Werkbeheerder:toekennenTechnicus{% endblock %}

{% block body %}
    <table class="table table-striped table-middle-aligned">
        <thead>
        <tr>
            <th scope="col">Technicus</th>
            <th scope="col" class="text-center"><i class="fa fa-cogs" aria-hidden="true"></i> Actions</th>
        </tr>
        </thead>
        <tbody>

        {% for technicus in technicussen %}
            <tr>
                <td>test</td>

                {% if is_granted('ROLE_WERKBEHEERDER') %}
                    <td class="text-center">
                        <div class="item-actions">
                            <a class="btn btn-sm btn-default">
                                Toekennen Technicus
                            </a>
                        </div>
                    </td>
                {% endif %}
           </tr>
        {% else %}
            <tr>
                <td colspan="4" align="center">geen locaties </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
