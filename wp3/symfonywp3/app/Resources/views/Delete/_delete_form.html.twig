{{ include('Delete/_delete_technicus_confirmation.html.twig') }}

<form action="{{ url('delete_technicus', {id: technicus.id}) }}" method="post" data-confirmation="true" id="delete-form">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-sm btn-danger">
        <i class="fa fa-trash" aria-hidden="true"></i>
        Delete
    </button>
</form>
